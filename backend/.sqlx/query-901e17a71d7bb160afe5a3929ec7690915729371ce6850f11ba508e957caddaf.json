{
  "db_name": "PostgreSQL",
  "query": "\n            WITH latest_positions AS (\n                SELECT DISTINCT ON (vehicle_id)\n                    vehicle_id,\n                    mta_id,\n                    stop_id,\n                    status,\n                    bearing,\n                    passengers,\n                    capacity,\n                    ST_X(geom) as lon,\n                    ST_Y(geom) as lat,\n                    recorded_at\n                FROM realtime.position\n                ORDER BY vehicle_id, recorded_at DESC\n            )\n            SELECT\n                t.id,\n                t.mta_id,\n                t.vehicle_id,\n                t.route_id,\n                t.direction,\n                t.created_at,\n                t.updated_at,\n                t.deviation,\n                CASE\n                    WHEN t.assigned IS NOT NULL THEN jsonb_build_object(\n                        'stop_id', p.stop_id,\n                        'status', p.status,\n                        'express', t.express,\n                        'assigned', t.assigned\n                    )\n                    ELSE jsonb_build_object(\n                        'stop_id', p.stop_id,\n                        'status', p.status,\n                        'lat', p.lat,\n                        'lon', p.lon,\n                        'bearing', p.bearing,\n                        'passengers', p.passengers,\n                        'capacity', p.capacity\n                    )\n                END AS data\n            FROM realtime.trip t\n            LEFT JOIN latest_positions p ON t.vehicle_id = p.vehicle_id\n            WHERE\n                t.updated_at >= (($1)::timestamp with time zone - INTERVAL '5 minutes')\n                AND t.id = ANY(\n                    SELECT t.id\n                    FROM realtime.trip t\n                    LEFT JOIN realtime.stop_time st ON st.trip_id = t.id\n                    WHERE st.arrival BETWEEN $1 AND ($1 + INTERVAL '4 hours')\n                )\n            ORDER BY t.created_at DESC\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "id",
        "type_info": "Uuid"
      },
      {
        "ordinal": 1,
        "name": "mta_id",
        "type_info": "Varchar"
      },
      {
        "ordinal": 2,
        "name": "vehicle_id",
        "type_info": "Varchar"
      },
      {
        "ordinal": 3,
        "name": "route_id",
        "type_info": "Varchar"
      },
      {
        "ordinal": 4,
        "name": "direction",
        "type_info": "Int2"
      },
      {
        "ordinal": 5,
        "name": "created_at",
        "type_info": "Timestamptz"
      },
      {
        "ordinal": 6,
        "name": "updated_at",
        "type_info": "Timestamptz"
      },
      {
        "ordinal": 7,
        "name": "deviation",
        "type_info": "Int4"
      },
      {
        "ordinal": 8,
        "name": "data",
        "type_info": "Jsonb"
      }
    ],
    "parameters": {
      "Left": [
        "Timestamptz"
      ]
    },
    "nullable": [
      false,
      false,
      false,
      false,
      true,
      false,
      false,
      true,
      null
    ]
  },
  "hash": "901e17a71d7bb160afe5a3929ec7690915729371ce6850f11ba508e957caddaf"
}
